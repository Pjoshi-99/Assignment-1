import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  textStyle: {
    fontSize: 20,
    color: 'black',
  },
  heading: {
    color: '#f0434c',
    fontSize: 28,
  },
});

export default styles;