import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  textStyle: {
    fontSize: 20,
    margin: 10,
    textAlign: 'justify',
    color: 'black',
  },
  heading: {
    fontSize: 35,
    textAlign: 'center',
    color: '#f0434c',
  },
});

export default styles;